<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             x:Class="ForeScore.Views.LoginPage"
             Shell.NavBarIsVisible="false"
             Shell.PresentationMode="ModalAnimated"
             BackgroundImage="{Binding SplashSource}"
             Title="{Binding Title}"
             >
    <StackLayout>
        <StackLayout Padding="15">

            <Label Text="Welcome to ForeSkins" HorizontalOptions="Center" 
                   FontSize="Large" TextColor="{DynamicResource colorPrimaryDark}"/>

        </StackLayout>

        <StackLayout  VerticalOptions="EndAndExpand">

            <Button x:Name="btnSignInSignOut" Text="Sign in" Clicked="OnSignInSignOut" 
                    Style="{DynamicResource stdButton}" 
                        VerticalOptions="EndAndExpand" Opacity="0.9"/>



            <Button x:Name="btnCached" Text="Use Offline Creds" Clicked="OnSignInSignOut" 
                    Style="{DynamicResource stdButton}" 
                        VerticalOptions="EndAndExpand"  Opacity="0.9"/>

        </StackLayout>
    </StackLayout>

</ContentPage>
